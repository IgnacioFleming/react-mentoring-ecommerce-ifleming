export type Category = {
  slug: string;
  name: string;
  url: string;
};

export type Product = {
  name: string;
  brand: string;
  price: number;
  discount: number;
  thumbnail: string;
  rating: number;
};
