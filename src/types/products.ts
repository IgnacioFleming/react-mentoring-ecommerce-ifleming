export type Category = {
  slug: string;
  name: string;
  url: string;
};

export type Product = {
  name: string;
  brand: string;
  rating: number;
  price: number;
  discount: number;
  thumbnail: string;
};
